# =================
# Node.js
# =================

# 1. 의존성 폴더 (라이브러리)
# npm install 시 자동으로 생성되며, 용량이 매우 크고 개별 PC 환경마다 다를 수 있음
# package.json만 있으면 언제든 똑같이 복구 가능하므로 절대 올리면 안 됨
/node_modules

# 2. 로그 파일
# 서버 실행 중 발생하는 각종 로그 파일들
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 3. 개발 환경 설정 파일
# 에디터(VSCode 등)가 자동으로 생성하는 설정 파일. 다른 개발자와 공유할 필요 없음
.vscode/

# =================
# 운영체제 (OS)
# =================

# 4. 각 운영체제가 자동으로 생성하는 시스템 파일
# macOS
.DS_Store
# Windows
Thumbs.db

# =================
# 민감 정보 및 환경 변수
# =================

# 5. 데이터베이스 비밀번호, API 키 등 민감한 정보가 담긴 파일
# **보안상 가장 중요!** 해킹의 주된 원인이 됨
.env

# =================
# Docker & 데이터베이스
# =================

# 6. Docker가 생성하는 데이터베이스 데이터 폴더
# 실제 DB 데이터가 저장되는 곳으로, 용량이 크고 개인의 테스트 데이터이므로 공유하지 않음
/db-data

# =================
# 기타
# =================

# 7. 빌드 결과물 폴더 (나중에 TypeScript 등을 사용할 경우)
/dist
/build 